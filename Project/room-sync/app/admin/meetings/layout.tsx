import { ReactNode } from "react";

export const metadata = {
  title: "Meetings | RoomSync Admin",
};

export default function MeetingsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
